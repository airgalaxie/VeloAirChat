name: VeloAirChatPaperBridge
version: ${version}
main: de.airgalaxie.veloairchat.paper.VeloAirChatPaperBridge
api-version: '26.2'
author: AirGalaxie
description: Paper platform adapter for the central Velocity core
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true
    LuckPerms:
      load: BEFORE
      required: false
      join-classpath: true
    dynmap:
      load: BEFORE
      required: false
      join-classpath: true
